[project]
name = "mcp-chatbot-client"
version = "0.1.0"
description = "Gemini-powered MCP chatbot client for multiple FastMCP servers"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
